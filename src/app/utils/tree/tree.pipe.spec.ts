/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TreePipe } from './tree.pipe';

describe('TreePipe', () => {
  it('create an instance', () => {
    let pipe = new TreePipe();
    expect(pipe).toBeTruthy();
  });
});
